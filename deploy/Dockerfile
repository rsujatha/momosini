# MOVED. The build Dockerfile now lives at the REPO ROOT (../Dockerfile), because
# `gcloud run deploy --source .` only auto-detects a Dockerfile at the source root.
# This stub is kept only so old references don't 404; do not build from it.
# See deploy/README.md for the deploy steps.
